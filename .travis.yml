sudo: false
language: node_js
node_js:
  - "stable"
  - "4.1"
before_install:
  - npm install -g gulp
  - npm install -g bower
  - npm install
  - bower install
script:
  - npm run-script ci
cache:
  directories:
    - node_modules
